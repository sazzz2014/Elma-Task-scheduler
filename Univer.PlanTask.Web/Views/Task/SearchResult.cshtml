@model IEnumerable<Univer.PlanTask.Web.Models.Task>



<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateStart)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateEnd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Money)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        var statusId = $"status_{item.Id}";
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateEnd)
            </td>
            <td id="@statusId">
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Money)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User)
            </td>
            <td>
                @Html.ActionLink("Изменить", "Edit", new { id = item.Id }) |

                @Html.ActionLink("Выполнить", "Done", new { id = item.Id }) |
               @* @if (item.Status != Univer.PlanTask.Web.Models.TaskStatus.Done)
                {

                    <button>Выполнить</button>
                    using (Ajax.BeginForm("Done", new AjaxOptions { UpdateTagetId = "DivSearchResult" }))
                    {
                        @Html.HiddenFor(model => item.Id)
                        <button class="btn btn-default" type="submit">Выполнить</button>
                    }
                }*@
            </td>
        </tr>
    }

</table>

<script>
    function taskStatusUpdate(evt) {
        console.log(evt);
    }
</script>