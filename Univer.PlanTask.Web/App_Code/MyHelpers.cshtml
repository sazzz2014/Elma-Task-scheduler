@helper HtmlButton(string name, string value)
{
    <input type="button" name="@name" value="@value" class="btn btn-default" />
}

@helper HtmlSubmit(string value)
{
    <input type="submit" value="@value" class="btn btn-success" />
}